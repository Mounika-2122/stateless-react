import React from 'react';
const college = (props) => {
   return (
      <div>
         <p> Hello Everyone {props.name} Good Afternoon cgcs.
         {props.course}</p>
         {props.student}
      </div>
   );
}
export default college;
